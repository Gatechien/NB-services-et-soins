{% extends 'base.html.twig' %}

{% block title %}Modifier une fréquence{% endblock %}

{% block body %}

    <a class="btn btn-dark mb-2" href="{{ path('app_frequency_index') }}">Retour</a>
        <h1 class="mt-2">Modifier une fréquence d'intervention</h1>
    {% if is_granted('ROLE_ADMIN') %}
        <div class="mx-1">
            {{ include('frequency/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
        <div class="mt-2">
            {{ include('frequency/_delete_form.html.twig') }}
        </div>
    {% endif %}
    
{% endblock %}